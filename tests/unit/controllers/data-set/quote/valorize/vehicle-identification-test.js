import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Controller | data-set/quote/valorize/vehicle-identification', function(hooks) {
  setupTest(hooks);

  // Replace this with your real tests.
  test('it exists', function(assert) {
    let controller = this.owner.lookup('controller:data-set/quote/valorize/vehicle-identification');
    assert.ok(controller);
  });
});
